import { Loading } from '.'

export default {
  title: 'General Components/Loading',
  component: Loading
}

const Template = () => <Loading isVisible={true}/>

export const LoadingComponent = Template.bind({})
