import { GreetingText } from '.'

export default {
  title: 'General Components/greetingText',
  component: GreetingText
}

const Template = () => <GreetingText/>

export const GreetingComponent = Template.bind({})
